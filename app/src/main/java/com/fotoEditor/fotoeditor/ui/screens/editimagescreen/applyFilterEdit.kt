package com.fotoEditor.fotoeditor.ui.screens.editimagescreen

import android.graphics.Bitmap
import android.graphics.Paint
import androidx.compose.ui.geometry.Rect
import androidx.compose.ui.graphics.ImageBitmap
import androidx.compose.ui.graphics.asAndroidBitmap
import androidx.compose.ui.graphics.asImageBitmap
import androidx.core.graphics.applyCanvas

class applyFilterEdit() {
    object applyFilter{

//        fun applyColorMatrix(bitmap: ImageBitmap, colorMatrix: android.graphics.ColorMatrix): ImageBitmap {
//            val androidBitmap = bitmap.asAndroidBitmap()
//
//            val resultBitmap = Bitmap.createBitmap(androidBitmap.width, androidBitmap.height, androidBitmap.config)
//
//            resultBitmap.applyCanvas { canvas ->
//                val paint = Paint().apply {
//                    isFilterBitmap = true
//                    colorFilter = ColorMatrixColorFilter(colorMatrix)
//                }
//                canvas.drawBitmap(androidBitmap, 0f, 0f, paint)
//            }
//
//            return Bitmap(resultBitmap)
//
//        }
    }

}